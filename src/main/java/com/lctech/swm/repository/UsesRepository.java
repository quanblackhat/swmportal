package com.lctech.swm.repository;

import org.springframework.data.repository.CrudRepository;

import com.lctech.swm.domain.Uses;

public interface UsesRepository extends CrudRepository<Uses, Long> {
}
